{
    "framework": null,
    "buildCommand": "npm run build",
    "outputDirectory": null,
    "installCommand": "npm install",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api/index.ts"
        }
    ],
    "functions": {
        "api/index.ts": {
            "memory": 1024,
            "maxDuration": 30
        }
    }
}